# Install from the codex-router folder:
#   python -m pip install -r requirements.txt
#
# Python >=3.13 is required by upstream codex-lb.
#
# System prerequisites not installable by pip:
# - uv for the bwtools native start wrapper.
# - Bun >=1.3.7 for the upstream frontend workspace
#   (`upstream/frontend/package.json` declares packageManager "bun@1.3.7").
# - Node.js/npm for the current bwtools dashboard build wrapper.
-e ./upstream
